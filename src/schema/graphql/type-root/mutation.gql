type Mutation {

    register(user: UserInput!): ResultRegister!

    # añadir los generos de los juegos

    addGenre(genre: String!): ResultGenre!
    updateGenre(genre: GenreInput!): ResultGenre!
}